package com.example.cheapsharkgames.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Standard dimensions and spacings used throughout the app to ensure consistency.
 */
object Dimensions {
    /**
     * Standard padding for screens and large containers.
     */
    val PaddingMedium = 16.dp

    /**
     * Small padding for tighter layouts.
     */
    val PaddingSmall = 8.dp

    /**
     * Extra small padding
     */
    val PaddingXSmall = 4.dp

    /**
     * Standard size for game thumbnail images in lists.
     */
    val GameThumbnailWidth = 120.dp

    /**
     * Standard size for game thumbnail images in lists.
     */
    val GameThumbnailHeight = 45.dp

    /**
     * Standard horizontal spacing between elements (e.g., image and text).
     */
    val SpacingMedium = 16.dp

    /**
     * Standard elevation for cards and elevated elements.
     */
    val ElevationSmall = 4.dp

    /**
     * Standard border stroke width.
     */
    val BorderThin = 1.dp
}
